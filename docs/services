- Portainer
Setup compose file, port is 9000.

- Adguard
Setup compose file, port is 3000. Set router's DNS to this so every device gets the adblocking.

- Nextcloud
Setup db and app, used .env for password. Port is 8080. Tested it on other devices to ensure it works.

- Immich
Setup server, db and machine learning. Port is 2283.

-Nginx
Port is 81.

-DuckDNS
Used DuckDNS to get a DDNS domain for nextcloud and immich. 
